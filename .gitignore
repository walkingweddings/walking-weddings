node_modules/

# Admin journal creator: uploaded user media (drafts ARE tracked so they
# survive Railway redeploys — see syncToGitHub calls in admin/server.js)
assets/images/journal/*
!assets/images/journal/.gitkeep

# Environment
.env
.env.local
